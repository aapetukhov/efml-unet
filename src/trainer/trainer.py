from __future__ import annotations

from src.trainer.base_trainer import BaseTrainer


class Trainer(BaseTrainer):
    """Keeps BaseTrainer behaviour; split exists to extend logging or hooks later."""

    pass
